# Vue inside Doker

> A Docker project

## Build Setup

``` bash
# Build image
docker build -t vue .
# Run image
docker run -p 80:8080 -d vue
# Test
curl localhost or search in your favourite browser localhost
